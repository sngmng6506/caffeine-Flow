{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "cd customer && npm ci --include=dev && npm run build && cd ../owner && npm ci --include=dev && VITE_GOOGLE_CLIENT_ID=\"$VITE_GOOGLE_CLIENT_ID\" VITE_NAVER_ENABLED=\"$VITE_NAVER_ENABLED\" npm run build && cd ../server && npm ci --omit=dev"
  },
  "deploy": {
    "preDeployCommand": ["npm run migrate --prefix server"],
    "startCommand": "node server/server.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
