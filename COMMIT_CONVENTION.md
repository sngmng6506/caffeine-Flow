# 커밋 메시지 컨벤션

AI 도구와 협업하는 모든 커밋에 적용한다. 커밋은 "무엇을"이 아니라 **"왜"와 "어떤 판단을"** 기록하는 문서다. CI에서 강제하지 않는 권장 규칙이다.

## 형식

```text
<type>(<scope>): <제목 — 한국어, 명사형, 72자 이내>

Why: <이 변경이 필요했던 배경·문제·요청. 1~3문장>

- <파일/모듈>: <변경 내용 요약>
- <핵심 구현 포인트 — 알고리즘, 자료구조, 파라미터>
- <테스트 추가 여부>

Decision: <이 방식을 택한 이유. 기각한 대안과 근거를 함께>

🤖 Generated with Claude Code
```

## 규칙

1. **type**: `feat` `fix` `refactor` `perf` `docs` `test` `chore` `style` `ci`
2. **scope**: 변경 영역. 이 저장소에서는 `server` `customer` `owner` `electron` `db` `security` `ui` 등
3. **Why는 필수다.** diff는 "무엇"을 보여주지만 "왜"는 커밋에만 남는다. 요청 배경, 재현된 버그, 사용자 피드백을 적는다.
4. **본문 bullet은 파일·모듈 단위로** 쓴다. 자잘한 나열보다 핵심 구현 판단을 우선한다.
5. **Decision은 선택지가 있었을 때 필수다.** 기각한 대안("A 대신 B — 이유")을 남겨 미래의 리뷰어가 같은 고민을 반복하지 않게 한다. 오타 수정처럼 판단이 없던 커밋은 생략한다.
6. **🤖 마커**를 AI가 생성·주도한 커밋에 붙인다. 도구에 따라 `Claude Code` / `Codex` / `Gemini` 등으로 표기한다.
7. **논리적 변경 1개 = 커밋 1개.** 보안 수정과 UI 수정을 한 커밋에 섞지 않는다.
